/**
 * Customize row-to-model property transforms.
 * This file is safe to edit and is not overwritten by cocogen update.
 */
import { PropertyTransformBase } from "./propertyTransformBase.js";

/**
 * User-overridable transforms for schema fields.
 */
export class PropertyTransform extends PropertyTransformBase {
  // Override transform methods to customize mapping. Example:
  // protected transformTitle(row: Record<string, unknown>): string {
  //   const value = super.transformTitle(row);
  //   return value;
  // }
  //
<% if (allowCustomItemAcl) { -%>
  // Override transformAcl(item) to build a per-item ACL from parsed model values.
  // The default grants Everyone access, preserving cocogen's standard behavior.
<% } else if (isPeopleConnector) { -%>
  // People connector ACL overrides are disabled in this generated output.
  // Re-run cocogen update with --use-preview-features before overriding transformAcl(item).
<% } -%>
}

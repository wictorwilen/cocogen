// Datasource contract for producing items to ingest.
using System.Collections.Generic;

namespace <%= namespaceName %>.Datasource;

/// <summary>
/// Contract for any datasource that yields items for ingestion.
/// Implement this interface to swap the default datasource for an API, database, or other system.
/// </summary>
public interface IItemSource<TItem>
{
    /// <summary>
    /// Stream items asynchronously for ingestion.
    /// </summary>
    IAsyncEnumerable<TItem> GetItemsAsync(CancellationToken cancellationToken = default);
}
